# Browsers that we support

# Production browsers - covers 95%+ of global users
> 0.5%
last 2 versions
not dead

# Specific browser versions for compatibility
Chrome >= 90
Firefox >= 88
Safari >= 14
Edge >= 90
iOS >= 14
Samsung >= 13

# Exclude very old browsers
not IE 11
not op_mini all
